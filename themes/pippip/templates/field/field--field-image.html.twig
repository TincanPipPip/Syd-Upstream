{% for item in items %}
  {% set image = image_style(item.content['#item'].entity.fid.value, ['teaser_thumb']) %}
  <img class="lazy" data-src="{{ image.teaser_thumb }}" height="660" width="840" alt="{{ item.content['#item'].alt }}" title="{{ item.content['#item'].title }}" />
{% endfor %}