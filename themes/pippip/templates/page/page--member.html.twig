{% extends 'page.html.twig' %}

{% block content %}
  <div class="grid">
    <div class="col-sm-12">
      <div class="m-entity">
        <h1>{{ page['#title'] }}</h1>

        {{ page.content }}
      </div>
    </div>
  </div>
{% endblock content %}