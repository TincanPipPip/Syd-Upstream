{% extends 'page.html.twig' %}

{% block content %}
  <div class="grid m-no-banner">
    <div class="col-sm-12">
      <h1>{{ page['#title'] }}</h1>

      {{ page.content }}
    </div>
  </div>
{% endblock content %}