{% set jpg= 'jpg' %}
{% set webp = 'webp' %}

{% set imageSm = image|image_style('banner_image_sm') %}
{% set imageMd = image|image_style('banner_image_md') %}
{% set imageBg = image|image_style('banner_image') %}

<picture>
	{# <source media="(max-width: 600px)" srcset="{{ imageSm|replace({ (jpg): webp }) }}" type="image/webp"> #}
	<source media="(max-width: 600px)" srcset="{{ imageSm }}" type="image/jpeg">
	{# <source media="(max-width: 1000px)" data-srcset="{{ imageMd|replace({ (jpg): webp }) }}" type="image/webp"> #}
	<source media="(max-width: 1000px)" data-srcset="{{ imageMd }}" type="image/jpeg">
	{# <source data-srcset="{{ imageBg|replace({ (jpg): webp }) }}" type="image/webp"> #}
	<source data-srcset="{{ imageBg }}" type="image/jpeg">
	<img data-src="{{ imageBig }}" alt="{{ alt }}" class="lazy" />
</picture>